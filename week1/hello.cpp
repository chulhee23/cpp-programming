// #= 전처리 명령
// include = 이후에 올 파일을 포함시키겠다.
// iostream = 라이브러리의 일종.스트림을 이용해 입출력 기능을 제공한다.Input Ouput STREAM 의 약자.
#include <iostream>

/*
표준 네임스페이스를 사용
STL(Standard TEmplate Library) 은 네임스페이스를 이용해서 만들어 졌습니다.
STL 의 네임스페이스가 std
표준 라이브러리는 다른 라이브러리와의 함수 이름, 클래스 이름등의 충돌을 막기 위해서 
네임스페이스를 쓰기로 결정
*/
using namespace std;

int main(void){
  cout << "Hello World!\n";
  return 0;
}